{
	"name": "perxel/perxel-toolkit",
	"description": "Small admin and block-editor features plus integrations for popular plugins - each one off until you turn it on.",
	"type": "wordpress-plugin",
	"license": "GPL-2.0-or-later",
	"require": {
		"php": ">=7.4"
	},
	"require-dev": {
		"dealerdirect/phpcodesniffer-composer-installer": "^1.0",
		"wp-coding-standards/wpcs": "^3.1",
		"phpcompatibility/phpcompatibility-wp": "^2.1"
	},
	"scripts": {
		"lint": [
			"bin/check-suppressions.sh",
			"phpcs"
		],
		"lint:fix": "phpcbf",
		"build": "bin/build-zip.sh",
		"build:dirty": "bin/build-zip.sh --dirty"
	},
	"config": {
		"allow-plugins": {
			"dealerdirect/phpcodesniffer-composer-installer": true
		}
	}
}
